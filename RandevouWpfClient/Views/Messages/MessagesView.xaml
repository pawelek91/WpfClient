<Window x:Class="RandevouWpfClient.Views.MessagesView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:RandevouWpfClient.Views"
        xmlns:vm="clr-namespace:RandevouWpfClient.ViewModels"
        mc:Ignorable="d"
        Title="MessagesView" Height="450" Width="800">
    <Window.DataContext>
        <vm:MessagesViewModel/>
    </Window.DataContext>
    <StackPanel>
        <TextBlock Text="Wiadomości" FontSize="20" />
        <ListView x:Name="LastMessagesListView" ItemsSource="{Binding LastMessages}">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <StackPanel>
                        <WrapPanel>
                            <Label Content="{Binding SpeakerName, Mode=TwoWay}" />
                            <Label Content="{Binding MessageDate, Mode=TwoWay}" />
                        </WrapPanel>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
            
        </ListView>
    </StackPanel>
</Window>
