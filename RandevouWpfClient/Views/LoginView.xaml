<Window x:Class="RandevouWpfClient.Views.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:RandevouWpfClient.Views"
        mc:Ignorable="d"
        Title="LoginView" Height="250" Width="400">
    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
        <Label x:Name="loginStatusLbl" Content="Nie zalogowany" />
        <Grid Width="240" Margin="10,10,10,10">
            <Grid.RowDefinitions>
                <RowDefinition Height="25" />
                <RowDefinition Height="25" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="90" />
                <ColumnDefinition Width="150" />
            </Grid.ColumnDefinitions>
            <Label Content="Login" Grid.Row="0" Grid.Column="0" />
            <Label Content="Hasło" Grid.Row="1" Grid.Column="0" />

            <TextBox x:Name="loginNameTxtBox" Grid.Row="0" Grid.Column="1" />
            <PasswordBox x:Name="loginPasswordTxtBox" PasswordChar="*"  Grid.Row="1" Grid.Column="1" />
        </Grid>
        
        <Button x:Name="loginBTN" Margin="0,30,0,10"  Content="Zaloguj" Width="150" />
        
    </StackPanel>
   
</Window>
